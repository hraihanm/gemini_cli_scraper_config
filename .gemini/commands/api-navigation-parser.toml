description = "API Phase 2 (generic): API navigation / listings parsers. Usage: /api-navigation-parser scraper=<name> [project=dmart-dloc] [auto_next=true]"

prompt = """
API Phase 2 — generic.

Firmware: `.gemini/system.md`.

@{docs/shared/agent-rules-gemini.md}
@{docs/shared/datahen-conventions.md}
@{docs/shared/selector-discovery.md}

Parse {{args}}: `scraper=`, `project=`, `out=`, `auto_next=`.

1. `read_file` → `profiles/<project>.toml`.
2. In **`api_pipeline.phases`**, find **`api-navigation-parser`** — read its **`workflow`** path.
3. `read_file` → workflow; execute exactly.
4. Auto-chain to next API phase when `auto_next=true` (use **`scripts/chain.ps1`** / **`scripts/chain.sh`**).
"""
