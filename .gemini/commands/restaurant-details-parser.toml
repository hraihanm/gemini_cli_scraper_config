description = "Phase 3 (dhero): Restaurant details parser. Generic path: project must be dhero. Usage: /restaurant-details-parser scraper=<name> project=dhero [url=...] [auto_next=true]"

prompt = """
DHero Phase 3 — restaurant details (session-independent).

Firmware: `.gemini/system.md`.

@{docs/shared/agent-rules-gemini.md}
@{docs/shared/datahen-conventions.md}
@{docs/shared/selector-discovery.md}

Parse {{args}}: `scraper=` (required), `project=` (must be **`dhero`**), `url=`, `resume-url=`, `out=`, `auto_next=`.

1. `read_file` → `profiles/dhero.toml`.
2. Find pipeline phase **`restaurant-details-parser`** — read its **`workflow`** path (authoritative).
3. `read_file` → that workflow; execute every STEP.
4. Auto-chain per agent rules + **`scripts/chain.ps1`** / **`scripts/chain.sh`** when `auto_next=true` (next phase is usually **`menu-parser`**).
"""
