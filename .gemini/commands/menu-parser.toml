description = "Phase 5 (dhero): Menu details parser (final). Extracts items from a single menu page. Usage: /menu-parser scraper=<name> project=dhero [url=...]"

prompt = """
DHero Phase 5 — menu details parser (**final** phase).

Firmware: `.gemini/system.md`.

@{docs/shared/agent-rules-gemini.md}
@{docs/shared/datahen-conventions.md}
@{docs/shared/selector-discovery.md}

Parse {{args}}: `scraper=`, `project=` (**dhero**), `url=`, `resume-url=`, `out=`.

1. `read_file` → `profiles/dhero.toml`.
2. Find phase **`menu-parser`** — read its **`workflow`** path (authoritative).
3. `read_file` → that workflow; execute every STEP.
4. **No auto-chain** (terminal phase). Ignore `auto_next` for chaining purposes.
"""
