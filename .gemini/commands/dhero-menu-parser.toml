description = "Alias: /menu-parser for dhero project. Final phase — extracts menu items per restaurant. Usage: /dhero-menu-parser scraper=<name> [url=<restaurant_url>] [auto_next=true]"

prompt = """
This is a project alias. You are running /menu-parser with project=dhero. This is the FINAL phase of the dhero pipeline.

🚨🚨🚨 **CRITICAL: YOU ARE IN GEMINI CLI - USE TOOLS DIRECTLY, NEVER GENERATE CODE** 🚨🚨🚨
- **STOP**: If you are about to write `import os`, `import json`, `print()`, `def `, or any Python/JavaScript code, STOP IMMEDIATELY
- **YOU ARE AN AI ASSISTANT IN GEMINI CLI** — You have direct access to MCP tools via tool calling
- **USE TOOLS DIRECTLY** — Call tools like `read_file`, `browser_navigate`, `parser_tester`, `write_file` using the tool interface
- **FORBIDDEN**: Never write Python scripts, JavaScript code, or any programming code
- **IF YOU START WRITING CODE**: Stop immediately and use tools instead

🚨 **CRITICAL: NEVER CALL BROWSER TOOLS VIA SHELL** 🚨
🚨 CRITICAL - ABSOLUTE PATHS REQUIRED FOR ALL WRITEFILE OPERATIONS 🚨
🚨 CRITICAL - STOP AFTER COMPLETION — THIS IS THE LAST PHASE, DO NOT CHAIN 🚨

---

## YOUR TASK

Parse {{args}}:
- `scraper=<scraper_name>` (REQUIRED)
- `url=<restaurant_url>` (OPTIONAL — sample restaurant URL for menu testing)
- `resume-url=<url>` (OPTIONAL)
- `out=<base_dir>` (OPTIONAL)
- `auto_next=true|false` — NOTE: LAST phase, no effect

Execute the /menu-parser workflow with `project=dhero`:

1. Read `docs/shared/agent-rules-gemini.md`
2. Read `docs/shared/datahen-conventions.md`
3. Read `docs/shared/selector-discovery.md`
4. Read `profiles/dhero.toml`
5. Read `docs/workflows/phases/04-menu-parser.md`
6. Follow every step in the workflow doc exactly
7. Display final completion message — DO NOT chain to any next command
"""
