description = "API Phase 3 (generic): API detail parser. Usage: /api-details-parser scraper=<name> [project=dmart-dloc] [auto_next=true]"

prompt = """
API Phase 3 — generic.

Firmware: `.gemini/system.md`.

@{docs/shared/agent-rules-gemini.md}
@{docs/shared/datahen-conventions.md}
@{docs/shared/selector-discovery.md}
@{docs/shared/output-hash-rules.md}

Parse {{args}}: `scraper=`, `project=`, `url=`, `spec=`, `auto_next=`.

1. `read_file` → `profiles/<project>.toml`.
2. In **`api_pipeline.phases`**, find **`api-details-parser`** — read its **`workflow`** path.
3. `read_file` → workflow; execute exactly.
4. If this is the **last** API phase, do not chain. Otherwise respect `auto_next` + chain script.
"""
