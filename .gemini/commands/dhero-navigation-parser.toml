description = "Alias: /navigation-parser for dhero project (restaurant listings). Usage: /dhero-navigation-parser scraper=<name> [resume-url=<url>] [out=<dir>] [auto_next=true]"

prompt = """
This is a project alias. You are running /navigation-parser with project=dhero.

🚨🚨🚨 **CRITICAL: YOU ARE IN GEMINI CLI - USE TOOLS DIRECTLY, NEVER GENERATE CODE** 🚨🚨🚨
- **STOP**: If you are about to write `import os`, `import json`, `print()`, `def `, or any Python/JavaScript code, STOP IMMEDIATELY
- **YOU ARE AN AI ASSISTANT IN GEMINI CLI** — You have direct access to MCP tools via tool calling
- **USE TOOLS DIRECTLY** — Call tools like `read_file`, `browser_navigate`, `parser_tester`, `write_file` using the tool interface
- **FORBIDDEN**: Never write Python scripts, JavaScript code, or any programming code
- **IF YOU START WRITING CODE**: Stop immediately and use tools instead

🚨 **CRITICAL: NEVER CALL BROWSER TOOLS VIA SHELL** 🚨
🚨 CRITICAL - ABSOLUTE PATHS REQUIRED FOR ALL WRITEFILE OPERATIONS 🚨
🚨 CRITICAL - STOP AFTER COMPLETION (if auto_next=false) 🚨

---

## YOUR TASK

Parse {{args}}:
- `scraper=<scraper_name>` (REQUIRED)
- `resume-url=<url>` (OPTIONAL)
- `out=<base_dir>` (OPTIONAL)
- `auto_next=true|false` (OPTIONAL, default: false)

Execute the full /navigation-parser workflow with `project=dhero`:

1. Read `docs/shared/agent-rules-gemini.md`
2. Read `docs/shared/datahen-conventions.md`
3. Read `docs/shared/selector-discovery.md`
4. Read `profiles/dhero.toml`
5. Read `docs/workflows/phases/02-navigation-parser.md`
6. Follow every step in the workflow doc exactly, using dhero profile values

Note for dhero: there are no categories or subcategories — the navigation parsers are listings.rb only.
The `boilerplate.parsers` list in dhero.toml starts with `listings.rb`.
Navigation parser for dhero = restaurant listing pages (list of restaurants) → queues restaurant detail pages.

7. Auto-chain to restaurant-details-parser if auto_next=true
"""
