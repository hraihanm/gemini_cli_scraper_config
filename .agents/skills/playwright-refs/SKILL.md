---
name: playwright-refs
description: Playwright snapshot refs (ref=e123) are NOT DOM attributes and must never be used as CSS selectors in Ruby parser code. Load if tempted to copy a Playwright ref into a selector, or when converting a snapshot reference into a real CSS selector.
---

This skill's knowledge is canonical in **`docs/shared/playwright-refs.md`**. `read_file` that document. Single source of truth — read it when this skill activates.
