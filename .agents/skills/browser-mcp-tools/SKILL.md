---
name: browser-mcp-tools
description: Playwright MCP Mod browser tool reference — browser_grep_html, browser_inspect_element, browser_verify_selector, browser_count_selector, browser_extract_images/json_ld, browser_detect_pagination, browser_network_* tools; cost-justification line required before expensive tools. Load when driving the browser for site discovery, selector work, or network/API inspection.
---

This skill's knowledge is canonical in **`docs/shared/browser-mcp-tools.md`**. `read_file` that document before browser interaction. Single source of truth — read it when this skill activates.
