---
name: greenfield-prompt-spec
description: Greenfield spec derivation — for project=greenfield the field-spec.json comes from the chat turn itself (slash args + all narrative text below the command), not a default disk file; optional spec=<path> override. Load when running /greenfield-scrape or building field-spec.json from a free-form user brief.
---

This skill's knowledge is canonical in **`docs/shared/greenfield-prompt-spec.md`**. `read_file` that document when handling a greenfield brief. Single source of truth — read it when this skill activates.
