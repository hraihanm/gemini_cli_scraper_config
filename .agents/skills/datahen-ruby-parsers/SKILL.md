---
name: datahen-ruby-parsers
description: Extended DataHen Ruby parser patterns — operational idioms for seeders, listings, and details parsers beyond the core conventions (pagination, dedup, begin/rescue error logging, safe JSON traversal). Load when implementing non-trivial parser logic or porting patterns from the legacy system.md.
---

This skill's knowledge is canonical in **`docs/shared/datahen-ruby-parsers.md`**. `read_file` that document. For core top-level-script rules and reserved variables it defers to the **datahen-conventions** skill (`docs/shared/datahen-conventions.md`) — read both when writing substantial parser logic.
