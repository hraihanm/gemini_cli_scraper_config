---
name: datahen-conventions
description: DataHen V3 parser conventions — parsers are top-level scripts, never redeclare pages/outputs/page/content, use pages<< / outputs<<, save_pages/save_outputs past 99 items, pre-loaded gems (nokogiri/json/digest/addressable/chronic), state file _log schema, error taxonomy. Load whenever writing, editing, or reviewing any DataHen parser .rb file or scraper state file.
---

This skill's knowledge is the canonical convention set in **`docs/shared/datahen-conventions.md`**. `read_file` that document and follow every rule in it. It is the single source of truth — do not paraphrase or cache; read it when this skill activates.
