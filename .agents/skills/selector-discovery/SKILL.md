---
name: selector-discovery
description: CSS selector discovery protocol using browser MCP tools — order is browser_grep_html → browser_inspect_element → browser_verify_selector/browser_evaluate; never use Playwright refs as CSS; JSON-LD/meta-tag/CSS extraction priority. Load when discovering or verifying selectors for any navigation, listings, or details parser field.
---

This skill's knowledge is canonical in **`docs/shared/selector-discovery.md`**. `read_file` that document and follow the discovery order for every field. Single source of truth — read it when this skill activates.
