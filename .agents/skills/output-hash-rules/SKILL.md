---
name: output-hash-rules
description: Output hash rules for details parsers — all 53 field-spec fields MUST appear, missing fields set to nil explicitly (never omitted), canonical field names (currency_code_lc, rank_in_listing, scraped_at_timestamp, crawled_source), nil-field summary warn before outputs<<. Load when writing or reviewing any details/details-style parser output hash.
---

This skill's knowledge is canonical in **`docs/shared/output-hash-rules.md`**. `read_file` that document before emitting any output hash. Single source of truth — read it when this skill activates.
